[tool.poetry]
name = "pyautoupdater"
version = "0.1.0"
description = ""
authors = ["yang, qingfeng <319511269@qq.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.7"
requests = "^2.28.0"
packaging = "^21.3"

[tool.poetry.extras]
security = ["cryptography", "python-gnupg"]
delta = ["bsdiff4"]


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
